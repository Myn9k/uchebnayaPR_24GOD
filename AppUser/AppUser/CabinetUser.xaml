<Window x:Class="AppUser.CabinetUser"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AppUser"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Кабинет пользователя пользователя" Height="1000" Width="800">
    <Grid Background="#ffdadada">
        <Border MinWidth="400" Margin="15" Background="White" VerticalAlignment="Center" Padding="20" MaxWidth="550" CornerRadius="20">
            <Border.Effect>
                <DropShadowEffect BlurRadius="30" Color="LightGray" ShadowDepth="0"/>
            </Border.Effect>
            <StackPanel>
                <TextBlock Text="Здравствуйте!" FontSize="20" FontWeight="Bold" VerticalAlignment="Top" HorizontalAlignment="Center" Margin="0 0 0 20"/>
                <TextBox x:Name="LoginUser" IsReadOnly="True" FontSize="15" materialDesign:HintAssist.Hint="Логин" FontWeight="Bold" VerticalAlignment="Top" Style="{StaticResource MaterialDesignFloatingHintTextBox}"/>
                <TextBox x:Name="EmailUser" IsReadOnly="True" FontSize="15" FontWeight="Bold" materialDesign:HintAssist.Hint="Почта" VerticalAlignment="Top" Style="{StaticResource MaterialDesignFloatingHintTextBox}"/>
                <TextBox x:Name="NewEmailTextBox" materialDesign:HintAssist.Hint="Новая почта" Style="{StaticResource MaterialDesignFloatingHintTextBox}"/>
                <TextBox x:Name="BirthdayTextBox" IsReadOnly="True" materialDesign:HintAssist.Hint="День рождения" Style="{StaticResource MaterialDesignFloatingHintTextBox}"/>
                <TextBox x:Name="FIOTextBox" IsReadOnly="True" materialDesign:HintAssist.Hint="ФИО" Style="{StaticResource MaterialDesignFloatingHintTextBox}"/>
                <Button Content="Сохранить почту" Click="SaveEmailButton_Click" HorizontalAlignment="Center"/>
                <StackPanel Margin="0 20">
                    <TextBlock Text="Дата события:"/>
                    <DatePicker x:Name="EventDatePicker" Width="200"/>
                </StackPanel>
                <TextBox x:Name="EventDescriptionTextBox" materialDesign:HintAssist.Hint="Описание события" Style="{StaticResource MaterialDesignFloatingHintTextBox}"/>
                <ListBox x:Name="EventsListBox" Width="300" Margin="0 20">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <TextBlock Text="{Binding Date}" FontWeight="Bold"/>
                                <TextBlock Text="{Binding Description}" TextWrapping="Wrap"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
                <StackPanel Orientation="Horizontal" Margin="20">
                    <Button Content="Сохранить событие" Click="SaveEventButton_Click" HorizontalAlignment="Left" Margin="20"/>
                    <Button Content="Удалить" Click="DeleteEventButton_Click" HorizontalAlignment="Right" Margin="20"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</Window>